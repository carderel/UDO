# Agents Index

Registry of agents installed in this project. `.agents/*.md` is the source of truth for every entry below; harness copies (for example `.claude/agents/`) are generated artifacts, regenerated by the resume protocol's Agent sync step. Never hand-edit a harness copy: `validate.py` flags drift between `.agents/` and the harness directory.

| name | source | tools | installed | synced-to-harness |
|------|--------|-------|-----------|--------------------|
| researcher | seed pack | file_read, web_search | yes | pending |
| data-auditor | seed pack | file_read, web_search, code_execution | yes | pending |
| strategist | seed pack | file_read | yes | pending |
| technical-writer | seed pack | file_read, file_write | yes | pending |

"synced-to-harness" moves to `yes` after the next resume protocol's Agent sync step confirms a harness copy exists and matches the `.agents/` source. New agents installed from `TOOLS/CATALOG-AGENTS.md` get a row here with source set to the catalog org.
