# Boot sequence

Read `AGENTS.md` in this folder and follow it before answering the first prompt. It is the canonical boot document for this project.

This file exists only because some harnesses look for a file named after themselves. It is a pointer, deliberately, so the protocol does not privilege one tool over another. Put project-specific notes in `AGENTS.md`, not here.
